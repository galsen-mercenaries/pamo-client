import { DisplayDateGaugePipe } from './display-date-gauge.pipe';

describe('DisplayDateGaugePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayDateGaugePipe();
    expect(pipe).toBeTruthy();
  });
});
